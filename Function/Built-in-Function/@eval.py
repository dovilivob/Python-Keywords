# Evaluate the specified expression, 
# if the expression is a legal statement, it will be executed

# eval(expression, globals, locals)

# Only eccepts a single expression

x = 'print(3)'
eval(x)
# Output: 3